
## 2024-05-18 - Missing ARIA Labels on Icon-Only Links
**Learning:** Icon-only social links (e.g., using FontAwesome icons) without `aria-label`s are completely opaque to screen readers, reading out nothing or generic unhelpful text like "link".
**Action:** Always verify icon-only interactive elements (links, buttons) have descriptive `aria-label`s.
