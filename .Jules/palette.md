## 2024-05-15 - [Add ARIA labels to social links]
**Learning:** Icon-only social links lack semantic meaning for screen readers without `aria-label` attributes.
**Action:** Add descriptive `aria-label` attributes (e.g., 'GitHub Profile', 'Twitter Profile', etc.) to all icon-only social links.
